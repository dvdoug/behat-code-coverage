default:
  extensions:
    LeanPHP\Behat\CodeCoverage\Extension:
      filter:
        whitelist:
          include:
            directories:
              'src': ~
      report:
        format: html
        options:
          target: build/coverage-behat
